[PACKAGE]:      SERVICE

[AVAILABLE COMMANDS]:          

    -service create :           Create Service File and Copy to /etc/systemd/system.

    -service delete :           Delete Service File in /etc/systemd/system.

    -service disable :          Disable Service.

    -service enable :           Enable Service.

    -service list.active :      List Only Active Services.

    -service list.all :         List All Services.

    -service new.production :   Turn on a container, create a new service, enable the service, 
                                start the service, and display status.

    -service reload :           Change Service Configuration Without Restart.

    -service restart :          Restart Service.

    -service start :            

